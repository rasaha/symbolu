# Guard inventory — `cloud-scaling-capacity-bounds-policy`

Generated by `scripts/cloud_scaling/guard_sweep.py --inventory-only`. Do not edit by
hand: CI regenerates this and fails on any difference.

**37 outcome-bearing guards.** A guard is a decision point whose body can
reach a refusal. What counts as a refusal differs by package and is recorded per guard
below: Phase 5A raises; Phase 5B also returns `_refuse(...)` at a gate and
`(_Outcome.X, …)` from the helper that decided it. Applying one package's definition to
another is not a stylistic choice — a raise-only reading of this package would miss
15 of the guards below.

Beyond the `if`/conditional-expression layer this package also inventories the
decision classes the guard-coverage ADR ratified, each with its own operator:

* **0 `else-arm`** — terminal `else` arms that refuse, neutralised by replacing the arm with `pass` (ADR §4.3). An `else` has no header, so `if False:` cannot reach it.
* **14 `helper-admission`** — statement-level calls to a raising helper, neutralised by deleting the call (ADR §4.2). Neutralising the helper's own `if` proves the check works, not that it is applied here.

## Reconciliation with the recorded inventories

This package records no prior inventory; this is the first one.

## Classification

Every guard is classified: **37 `SCORED`** — the
sweep neutralises it and the suite must fail — and
**0 `EXCLUDED`**, each with a reason from a closed vocabulary and
a test that measures the reason. A guard is never excluded because it survived; a
survivor with no prior declaration fails the sweep.

No guard in this package is excluded: every one is scored.

## Not counted, and why

* **0 `except` arms** that raise. The `if False:` operator
  cannot neutralise a handler, so they are outside this operator rather than overlooked.
* **10 extra sub-terms** of boolean guards. `if a and b:` is
  neutralised and scored as one guard; scoring each side independently is a different
  operator.

## Every guard

| # | Module:line | Kind | Shape | Class | Recorded in | Condition |
|---|---|---|---|---|---|---|
| 1 | `policy.py:64` | if | raise | SCORED | — | `not isinstance(value, str)` |
| 2 | `policy.py:68` | if | raise | SCORED | — | `not allow_empty and (not value.strip())` |
| 3 | `policy.py:76` | if | raise | SCORED | — | `not isinstance(value, str) or len(value) != 64 or any((c not in _HEX for c …` |
| 4 | `policy.py:92` | if | raise | SCORED | — | `isinstance(value, bool) or not isinstance(value, int)` |
| 5 | `policy.py:97` | if | raise | SCORED | — | `value < 0` |
| 6 | `policy.py:105` | if | raise | SCORED | — | `not isinstance(value, datetime)` |
| 7 | `policy.py:109` | if | raise | SCORED | — | `value.tzinfo is None or value.tzinfo.utcoffset(value) is None` |
| 8 | `policy.py:133` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.action_type, 'CapacityBound.action_type')` |
| 9 | `policy.py:134` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.resource_class, 'CapacityBound.resource_class', allow_e…` |
| 10 | `policy.py:135` | helper-admission | helper-admission call | SCORED | — | `_require_bound_magnitude(self.max_permitted_magnitude, 'CapacityBound.max_p…` |
| 11 | `policy.py:138` | helper-admission | helper-admission call | SCORED | — | `_require_bound_magnitude(self.max_permitted_delta, 'CapacityBound.max_permi…` |
| 12 | `policy.py:141` | if | raise | SCORED | — | `self.max_permitted_delta > self.max_permitted_magnitude` |
| 13 | `policy.py:177` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.policy_id, 'CapacityBoundsPolicyMetadata.policy_id')` |
| 14 | `policy.py:178` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.version, 'CapacityBoundsPolicyMetadata.version')` |
| 15 | `policy.py:179` | helper-admission | helper-admission call | SCORED | — | `_require_digest(self.content_digest, 'CapacityBoundsPolicyMetadata.content_…` |
| 16 | `policy.py:182` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.scope, 'CapacityBoundsPolicyMetadata.scope')` |
| 17 | `policy.py:183` | if | raise | SCORED | — | `self.scope not in SUPPORTED_POLICY_SCOPES` |
| 18 | `policy.py:188` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.lifecycle_state, 'CapacityBoundsPolicyMetadata.lifecycl…` |
| 19 | `policy.py:191` | if | raise | SCORED | — | `self.lifecycle_state not in SUPPORTED_LIFECYCLE_STATES` |
| 20 | `policy.py:197` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.tenant_id, 'CapacityBoundsPolicyMetadata.tenant_id', al…` |
| 21 | `policy.py:200` | helper-admission | helper-admission call | SCORED | — | `_require_token(self.supersedes_ref, 'CapacityBoundsPolicyMetadata.supersede…` |
| 22 | `policy.py:209` | if | raise | SCORED | — | `self.scope == POLICY_SCOPE_GLOBAL and self.tenant_id != ''` |
| 23 | `policy.py:214` | if | raise | SCORED | — | `self.scope == POLICY_SCOPE_TENANT and (not self.tenant_id.strip())` |
| 24 | `policy.py:222` | if | raising-helper call | SCORED | — | `value is not None` |
| 25 | `policy.py:223` | helper-admission | helper-admission call | SCORED | — | `_require_tzaware(value, f'CapacityBoundsPolicyMetadata.{name}')` |
| 26 | `policy.py:224` | if | raise | SCORED | — | `self.effective_from is not None and self.effective_to is not None and (self…` |
| 27 | `policy.py:254` | if | raise | SCORED | — | `not isinstance(self.metadata, CapacityBoundsPolicyMetadata)` |
| 28 | `policy.py:259` | if | raise | SCORED | — | `not isinstance(self.bounds, tuple)` |
| 29 | `policy.py:265` | if | raise | SCORED | — | `not self.bounds` |
| 30 | `policy.py:271` | if | raise | SCORED | — | `type(bound) is not CapacityBound` |
| 31 | `policy.py:280` | if | raise | SCORED | — | `bound.selector in seen` |
| 32 | `policy.py:298` | helper-admission | helper-admission call | SCORED | — | `_require_token(action_type, 'bound_for(action_type)')` |
| 33 | `policy.py:299` | helper-admission | helper-admission call | SCORED | — | `_require_token(resource_class, 'bound_for(resource_class)', allow_empty=True)` |
| 34 | `adapter.py:60` | if | raise | SCORED | — | `not isinstance(metadata, CapacityBoundsPolicyMetadata)` |
| 35 | `adapter.py:101` | if | raise | SCORED | — | `type(artifact) is not CapacityBoundsPolicy` |
| 36 | `adapter.py:110` | if | raise | SCORED | — | `not isinstance(metadata, CapacityBoundsPolicyMetadata)` |
| 37 | `adapter.py:150` | if | raise | SCORED | — | `not isinstance(metadata, dict) or 'content_digest' not in metadata` |
