# Guard inventory — `cloud-scaling-risk-integration`

Generated by `scripts/cloud_scaling/guard_sweep.py --inventory-only`. Do not edit by
hand: CI regenerates this and fails on any difference.

**100 outcome-bearing guards.** A guard is a decision point whose body can
reach a refusal. What counts as a refusal differs by package and is recorded per guard
below: Phase 5A raises; Phase 5B also returns `_refuse(...)` at a gate and
`(_Outcome.X, …)` from the helper that decided it. Applying one package's definition to
another is not a stylistic choice — a raise-only reading of this package would miss
36 of the guards below.

Beyond the `if`/conditional-expression layer this package also inventories the
decision classes the guard-coverage ADR ratified, each with its own operator:

* **1 `else-arm`** — terminal `else` arms that refuse, neutralised by replacing the arm with `pass` (ADR §4.3). An `else` has no header, so `if False:` cannot reach it.
* **10 `except-arm`** — `except` arms that return a typed rejection, neutralised by collapsing the reason member to a sentinel (ADR §4.1). The refusal survives and only the reason moves, so a suite that asks only whether *something* was rejected cannot see the mutation.
* **15 `helper-admission`** — statement-level calls to a raising helper, neutralised by deleting the call (ADR §4.2). Neutralising the helper's own `if` proves the check works, not that it is applied here.

## Static sites that decide more than one invariant

Guard-coverage ADR §7.2, ruled at ratification: a guard inside a loop over a
fixed set of flags is **one** static site with a recorded semantic
multiplicity, not unrolled into one scored site per flag. One mutation
neutralises all of them together, so a kill shows only that *at least one* is
tested — the discrimination burden falls on the suite (§6's within-class
criterion), not on this count. Each multiplicity below is read from the
iterated constant in the source, not recorded by hand.

| Module:line | Decides | Iterated over |
|---|---|---|
| `outcomes.py:118` | 7 invariants | `getattr(self, flag) is not False` |
| `outcomes.py:136` | 6 invariants | `getattr(self.decision, flag) is not False` |
| `authenticity.py:267` | 8 invariants | `_token_field(record, flag, kind) is not False` |
| `projection.py:127` | 9 invariants | `getattr(self, flag) is not False` |

So this package's 100 static guard sites decide 126 invariants in total.

## Mint coverage, and what it does not cover

The mint counter wraps `ugence_cloud_scaling_risk_integration.projection:project_recommendation`. Coverage is **partial**, and the
uncovered mints are named here rather than left for a reader to notice — an
undisclosed partial count is worse than a disclosed one (ADR §5):

* `ugence_cloud_scaling_risk_integration.authenticity:authenticate_controller_output` — the package's second module-level mint. `mint_site` names one function, and the projection is the artifact the later gates are about, so the authenticated token is counted only where it flows into a projection. A guard that is the last obstacle before an *authenticated token* and not before a projection is therefore not reported as minting.
* `ugence_cloud_scaling_risk_integration.adapter:CloudScalingRiskOutcome(status=RISK_DECISION, …) at adapter.py:276` — the terminal decision mint, and an inline class construction rather than a call to a name. ADR §5 rules it must **not** be wrapped: wrapping it would break `isinstance` and the dataclass path, and a mint counter that changes the program under test measures the instrumentation. The two other constructions in this module, at lines 312 and 356, are the rejection and abstention outcomes — refusals, not mints.

## Reconciliation with the recorded inventories

This package records no prior inventory; this is the first one.

## Classification

Every guard is classified: **100 `SCORED`** — the
sweep neutralises it and the suite must fail — and
**0 `EXCLUDED`**, each with a reason from a closed vocabulary and
a test that measures the reason. A guard is never excluded because it survived; a
survivor with no prior declaration fails the sweep.

No guard in this package is excluded: every one is scored.

## Not counted, and why

* **8 `except` arms** that raise. The `if False:` operator
  cannot neutralise a handler, so they are outside this operator rather than overlooked.
* **12 extra sub-terms** of boolean guards. `if a and b:` is
  neutralised and scored as one guard; scoring each side independently is a different
  operator.

## Every guard

| # | Module:line | Kind | Shape | Class | Recorded in | Condition |
|---|---|---|---|---|---|---|
| 1 | `identifiers.py:68` | if | raise | SCORED | — | `_CONTROLLER_ACTION_TYPES != CANONICAL_ACTION_TYPES` |
| 2 | `identifiers.py:85` | if | raise | SCORED | — | `not isinstance(action_kind, ActionKind)` |
| 3 | `identifiers.py:88` | if | raise | SCORED | — | `value not in CANONICAL_ACTION_TYPES` |
| 4 | `outcomes.py:118` | if | raise | SCORED | — | `getattr(self, flag) is not False` |
| 5 | `outcomes.py:123` | if | raise | SCORED | — | `not isinstance(self.status, AdapterOutcomeStatus)` |
| 6 | `outcomes.py:125` | if | raise | SCORED | — | `self.schema_version != ADAPTER_OUTCOME_SCHEMA_VERSION` |
| 7 | `outcomes.py:130` | if | raise | SCORED | — | `self.status is AdapterOutcomeStatus.RISK_DECISION` |
| 8 | `outcomes.py:131` | if | raise | SCORED | — | `not isinstance(self.decision, SubjectRiskDecision)` |
| 9 | `outcomes.py:136` | if | raise | SCORED | — | `getattr(self.decision, flag) is not False` |
| 10 | `outcomes.py:141` | if | raise | SCORED | — | `self.rejection_reason is not None or self.abstention_reason is not None` |
| 11 | `outcomes.py:145` | if | raise | SCORED | — | `self.status is AdapterOutcomeStatus.PROJECTION_ABSTAINED_UPSTREAM` |
| 12 | `outcomes.py:146` | if | raise | SCORED | — | `self.decision is not None or self.projection is not None` |
| 13 | `outcomes.py:151` | if | raise | SCORED | — | `not isinstance(self.abstention_reason, str) or self.abstention_reason == ''` |
| 14 | `outcomes.py:155` | if | raise | SCORED | — | `self.rejection_reason is not None` |
| 15 | `outcomes.py:160` | if | raise | SCORED | — | `self.decision is not None` |
| 16 | `outcomes.py:164` | if | raise | SCORED | — | `not isinstance(self.rejection_reason, AdapterRejectionReason)` |
| 17 | `outcomes.py:168` | if | raise | SCORED | — | `self.abstention_reason is not None` |
| 18 | `authenticity.py:216` | helper-admission | helper-admission call | SCORED | — | `_validate_authenticated_recommendation(self)` |
| 19 | `authenticity.py:242` | helper-admission | helper-admission call | SCORED | — | `_validate_authenticated_abstention(self)` |
| 20 | `authenticity.py:267` | if | raise | SCORED | — | `_token_field(record, flag, kind) is not False` |
| 21 | `authenticity.py:274` | if | raise | SCORED | — | `isinstance(value, bool) or not isinstance(value, str)` |
| 22 | `authenticity.py:276` | if | raise | SCORED | — | `not _DIGEST_RE.match(value)` |
| 23 | `authenticity.py:300` | if | raise | SCORED | — | `value is _MISSING` |
| 24 | `authenticity.py:319` | if | raise | SCORED | — | `type(token) is not expected` |
| 25 | `authenticity.py:357` | helper-admission | helper-admission call | SCORED | — | `_require_exact_token(token, AuthenticatedRecommendation, 'an authenticated …` |
| 26 | `authenticity.py:358` | helper-admission | helper-admission call | SCORED | — | `_assert_no_authority_fields(token, 'an authenticated recommendation')` |
| 27 | `authenticity.py:361` | if | raise | SCORED | — | `type(recommendation) is not CapacityActionRecommendation` |
| 28 | `authenticity.py:376` | if | raise | SCORED | — | `digest != recomputed` |
| 29 | `authenticity.py:397` | helper-admission | helper-admission call | SCORED | — | `_require_exact_token(token, AuthenticatedAbstention, 'an authenticated abst…` |
| 30 | `authenticity.py:398` | helper-admission | helper-admission call | SCORED | — | `_assert_no_authority_fields(token, 'an authenticated abstention')` |
| 31 | `authenticity.py:401` | if | raise | SCORED | — | `type(abstention) is not RecommendationAbstention` |
| 32 | `authenticity.py:408` | if | raise | SCORED | — | `carried is not None` |
| 33 | `authenticity.py:411` | if | raise | SCORED | — | `digest != recomputed` |
| 34 | `authenticity.py:428` | if | raising-helper call | SCORED | — | `token_type is AuthenticatedRecommendation` |
| 35 | `authenticity.py:429` | helper-admission | helper-admission call | SCORED | — | `_validate_authenticated_recommendation(token)` |
| 36 | `authenticity.py:430` | if | raising-helper call | SCORED | — | `token_type is AuthenticatedAbstention` |
| 37 | `authenticity.py:431` | helper-admission | helper-admission call | SCORED | — | `_validate_authenticated_abstention(token)` |
| 38 | `authenticity.py:432` | else-arm | raise | SCORED | — | `else of: token_type is AuthenticatedAbstention` |
| 39 | `authenticity.py:474` | if | raising-helper call | SCORED | — | `carried is not None` |
| 40 | `authenticity.py:479` | if | raising-helper call | SCORED | — | `expected is not None` |
| 41 | `authenticity.py:484` | if | raise | SCORED | — | `not expectations` |
| 42 | `authenticity.py:492` | if | raise | SCORED | — | `value != recomputed` |
| 43 | `authenticity.py:529` | if | raise | SCORED | — | `isinstance(source, _CONTROLLER_TYPES)` |
| 44 | `authenticity.py:543` | if | raise | SCORED | — | `type(value) is not expected` |
| 45 | `authenticity.py:581` | if | raising-helper call | SCORED | — | `expected_recommendation_digest is not None` |
| 46 | `authenticity.py:582` | helper-admission | helper-admission call | SCORED | — | `_require_digest_syntax('expected_recommendation_digest', expected_recommend…` |
| 47 | `authenticity.py:593` | if | raising-helper call | SCORED | — | `source_type is not CapacityActionRecommendation and source_type is not Reco…` |
| 48 | `authenticity.py:599` | helper-admission | helper-admission call | SCORED | — | `_reject_subclass(source)` |
| 49 | `authenticity.py:602` | if | raising-helper call | SCORED | — | `source_type is CapacityActionRecommendation` |
| 50 | `authenticity.py:617` | if | raising-helper call | SCORED | — | `source_type is RecommendationAbstention` |
| 51 | `authenticity.py:621` | if | raising-helper call | SCORED | — | `expected_recommendation_digest is not None` |
| 52 | `authenticity.py:635` | if | raise | SCORED | — | `not isinstance(source, Mapping)` |
| 53 | `authenticity.py:642` | if | raise | SCORED | — | `tag is None` |
| 54 | `authenticity.py:650` | if | raise | SCORED | — | `tag == RECOMMENDATION_SCHEMA_VERSION` |
| 55 | `authenticity.py:665` | helper-admission | helper-admission call | SCORED | — | `_require_exact_reconstruction(recommendation, CapacityActionRecommendation)` |
| 56 | `authenticity.py:679` | if | raise | SCORED | — | `tag == RECOMMENDATION_ABSTENTION_SCHEMA_VERSION` |
| 57 | `authenticity.py:686` | helper-admission | helper-admission call | SCORED | — | `_require_exact_reconstruction(abstention, RecommendationAbstention)` |
| 58 | `authenticity.py:689` | if | raising-helper call | SCORED | — | `carried is not None or expected_recommendation_digest is not None` |
| 59 | `projection.py:127` | if | raise | SCORED | — | `getattr(self, flag) is not False` |
| 60 | `projection.py:131` | if | raise | SCORED | — | `self.schema_version != PROJECTION_SCHEMA_VERSION` |
| 61 | `projection.py:135` | if | raise | SCORED | — | `not isinstance(self.request, SubjectRiskEvaluationRequestV2)` |
| 62 | `projection.py:139` | if | raise | SCORED | — | `self.request.evaluation_time is not None` |
| 63 | `projection.py:149` | if | raise | SCORED | — | `self.context.digest() != self.context_digest` |
| 64 | `projection.py:151` | if | raise | SCORED | — | `self.binding.digest() != self.subject_digest` |
| 65 | `projection.py:153` | if | raise | SCORED | — | `self.request.digest() != self.request_digest` |
| 66 | `projection.py:161` | if | raise | SCORED | — | `validation.context_digest != self.context_digest` |
| 67 | `projection.py:163` | if | raise | SCORED | — | `validation.subject_digest != self.subject_digest` |
| 68 | `projection.py:165` | if | raise | SCORED | — | `validation.recommendation_digest != self.recommendation_digest` |
| 69 | `projection.py:202` | if | raise | SCORED | — | `not isinstance(value, datetime)` |
| 70 | `projection.py:204` | if | raise | SCORED | — | `value.tzinfo is None or value.utcoffset() is None` |
| 71 | `projection.py:213` | if | raise | SCORED | — | `not isinstance(value, str) or value == ''` |
| 72 | `projection.py:252` | if | raise | SCORED | — | `candidates[0][1] is None` |
| 73 | `projection.py:257` | if | raise | SCORED | — | `value is None` |
| 74 | `projection.py:261` | if | raise | SCORED | — | `not isinstance(value, str) or not value.startswith('sha256:')` |
| 75 | `projection.py:289` | if | raise | SCORED | — | `type(authenticated) is not AuthenticatedRecommendation` |
| 76 | `projection.py:357` | if | raise | SCORED | — | `correlation_id is not None and (not isinstance(correlation_id, str))` |
| 77 | `adapter.py:113` | if | raise | SCORED | — | `seam is None` |
| 78 | `adapter.py:118` | if | raise | SCORED | — | `not callable(getattr(seam, 'evaluate', None))` |
| 79 | `adapter.py:122` | if | raise | SCORED | — | `clock is None or not callable(clock)` |
| 80 | `adapter.py:152` | helper-admission | helper-admission call | SCORED | — | `_validate_authenticated_output(authenticated)` |
| 81 | `adapter.py:153` | if | raise | SCORED | — | `type(authenticated) is AuthenticatedAbstention` |
| 82 | `adapter.py:186` | helper-admission | helper-admission call | SCORED | — | `_validate_authenticated_output(authenticated)` |
| 83 | `adapter.py:187` | except-arm | typed-refusal return | SCORED | — | `except UnsupportedRecommendationSourceError: self._rejected(AdapterRejectio…` |
| 84 | `adapter.py:191` | except-arm | typed-refusal return | SCORED | — | `except RecommendationInputError: self._rejected(AdapterRejectionReason.MALF…` |
| 85 | `adapter.py:195` | except-arm | typed-refusal return | SCORED | — | `except MissingIndependentDigestError: self._rejected(AdapterRejectionReason…` |
| 86 | `adapter.py:199` | except-arm | typed-refusal return | SCORED | — | `except RecommendationAuthenticityError: self._rejected(AdapterRejectionReas…` |
| 87 | `adapter.py:205` | if | typed-refusal call | SCORED | — | `type(authenticated) is AuthenticatedAbstention` |
| 88 | `adapter.py:211` | except-arm | typed-refusal return | SCORED | — | `except RecommendationInputError: self._rejected(AdapterRejectionReason.UNSU…` |
| 89 | `adapter.py:222` | except-arm | typed-refusal return | SCORED | — | `except RecommendationAuthenticityError: self._rejected(AdapterRejectionReas…` |
| 90 | `adapter.py:230` | except-arm | typed-refusal return | SCORED | — | `except ProjectionError: self._rejected(AdapterRejectionReason.PROJECTION_FA…` |
| 91 | `adapter.py:242` | except-arm | typed-refusal return | SCORED | — | `except AdapterConfigurationError: self._rejected(AdapterRejectionReason.UNT…` |
| 92 | `adapter.py:247` | helper-admission | helper-admission call | SCORED | — | `_require_within_validity(now, projection)` |
| 93 | `adapter.py:248` | except-arm | typed-refusal return | SCORED | — | `except RecommendationNotYetValidError: self._rejected(AdapterRejectionReaso…` |
| 94 | `adapter.py:254` | except-arm | typed-refusal return | SCORED | — | `except RecommendationValidityError: self._rejected(AdapterRejectionReason.R…` |
| 95 | `adapter.py:266` | if | raise | SCORED | — | `request.evaluation_time is not None` |
| 96 | `adapter.py:271` | if | raise | SCORED | — | `not isinstance(decision, SubjectRiskDecision)` |
| 97 | `adapter.py:289` | if | raise | SCORED | — | `not isinstance(now, datetime)` |
| 98 | `adapter.py:291` | if | raise | SCORED | — | `now.tzinfo is None or now.utcoffset() is None` |
| 99 | `adapter.py:379` | if | raise | SCORED | — | `now > projection.valid_until` |
| 100 | `adapter.py:384` | if | raise | SCORED | — | `now < projection.valid_from` |
