"""Distribution version for ``ugence-cloud-scaling-risk-integration`` (Phase 4C).

Read statically by the build backend so packaging never has to import the package
(and thus its two first-party dependencies) to resolve the version.
"""

from __future__ import annotations

__version__ = "0.1.0"

__all__ = ["__version__"]
