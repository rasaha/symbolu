"""Distribution version for ``ugence-cloud-scaling-authorization-contracts`` (Phase 5A).

Read statically by the build backend so packaging never has to import the package (and
thus its first-party dependencies) to resolve the version.
"""

from __future__ import annotations

__version__ = "0.3.0"

__all__ = ["__version__"]
