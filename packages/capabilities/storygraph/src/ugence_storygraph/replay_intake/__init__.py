"""Historical-replay intake package data (schemas + sanitized templates).

Contains no runtime logic — only the replay-record JSON Schema and the
sanitized intake templates that ship with the capability so consumers can map
enterprise event streams into the deterministic replay contract.
"""
