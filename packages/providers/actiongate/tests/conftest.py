"""Package test configuration. The shared kernel lifecycle harness lives in
:mod:`lifecycle_harness` (importable because the package ``conftest.py`` puts this
``tests`` directory on ``sys.path``)."""
