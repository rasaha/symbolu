# Alembic configuration for DilChat.
# The database URL is resolved at runtime from DILCHAT_DATABASE_URL (see migrations/env.py);
# it is intentionally NOT stored here so no connection string is committed.
[alembic]
script_location = migrations
prepend_sys_path = src
path_separator = os
