"""Edge Prioritization Experiment v0.1 — HybridRelationshipResolver Experimental v0.3.

Adds ONLY a deterministic Edge Prioritization layer between the (bit-identical v0.2)
validated graph and the (frozen) GraphTraversal governance + packet builder. It
influences governance solely by ordering the governance-input graph in the full
resolve() pipeline; discovery, Mode G, and Mode P are structurally unchanged.
Nothing in v0.2, the v0.1 experiment, or the frozen platform is modified."""
