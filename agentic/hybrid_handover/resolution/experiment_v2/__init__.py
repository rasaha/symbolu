"""Proposal Validation Experiment v0.1 — HybridRelationshipResolver Experimental v0.2.

Adds ONLY a deterministic Proposal Validation Layer between the (unchanged) v0.1
proposal generator and the (frozen) GraphTraversal governance + packet builder.
Nothing in the v0.1 experiment, the frozen resolvers, the parser, the metrics, or
the hidden corpus is modified. All reuse is read-only composition."""
