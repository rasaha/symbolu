"""Governance Semantics Experiment v0.1 — HybridRelationshipResolver Experimental v0.4.

Adds ONLY an experimental Governance Semantics Layer between the (bit-identical v0.2)
validated graph and the (frozen) packet builder, plus a documented adapter that
translates the semantic governance result into the frozen packet's single-primary
input contract. Discovery, classification, proposal validation, and packet Mode P are
reused unchanged. Nothing in v0.1/v0.2/v0.3 or the frozen platform is modified."""
