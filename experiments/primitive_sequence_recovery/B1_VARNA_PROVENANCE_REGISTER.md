# B1 — Varṇa-Mechanism Provenance Register (docs/data-only, Part E Step 1)

**Readiness verdict: `BLOCKED_BY_SOURCE_CONTRADICTIONS`.** This is a provenance *audit* of the CURRENT active
varṇa mechanism — it classifies where every pole, rule, and composition assumption comes from. It authors no
meaning, changes no mapping, runs no experiment, and freezes nothing. **Structure, not validated meaning.** No
`GENUTILITY_*`; no `ONTOLOGICAL_SIGNAL`; no ontology / semantic-truth / Sanskrit-privilege / generation-utility
claim. B1.4b′ remains `NULL_RETURN_BOTTOM`; Track B blocked; run01, Track G, and all frozen mappings unchanged.

Generated by `b1_varna_provenance_register.py` reading three read-only sources:
`frozen/varna_polarity_table_v3.json` (poles + `attested_vs_authored` + meta caveats), the **active** decomposition
`varna_bridge_active.word_to_varnas` (`bridge_v2_plus_theta_eth_ta`), and the facet render map
`build_b1_10_control_ext.VARNA_PLAIN` (11 varṇas). Machine-readable outputs in `varna_provenance_register/`.

---

## 0. Provenance statuses

| status | meaning |
|---|---|
| `PRIMARY_ATTESTED` | stated by the operator-supplied primary classical text |
| `SECONDARY_ATTESTED` | the vṛtti **name** is attested; the **pole reading** is the lexicon's, not a primary-text pole contrast |
| `INFERRED` | an attested faculty/direction exists, but the binding↔liberating **split** applies the framework's principle |
| `AUTHORED_PROVISIONAL` | pole text lexicon/researcher-supplied; no primary-text support |
| `CONTRADICTORY` | two source artifacts (table / meta caveat / code) assert conflicting facts |
| `MISSING` | inventory category carrying **no** meaning in the current mechanism |
| `OUT_OF_SCOPE` | deliberately excluded from the current mechanism |
| `UNRESOLVED` | flagged in-source as an open question, not settled |

---

## A. Varṇa inventory

**34 consonants classified; 0 vowels / anusvāra / visarga.** The mechanism is consonant-only.

### A.1 Missing inventory categories (carry no meaning today)

| category | members | status |
|---|---|---|
| vowels | a ā i ī u ū ṛ ṝ ḷ e ai o au | `MISSING` |
| anusvāra | ṃ (ं) | `MISSING` |
| visarga | ḥ (ः) | `MISSING` |

These are **`MISSING`, not `OUT_OF_SCOPE`**: the decomposer emits no vowel/anusvāra/visarga varṇa and the table
holds none, so any distinction they would carry is discarded. Per the guardrails, **no meaning is added here.**

### A.2 Pole provenance — the central asymmetry

68 poles (34 × binding/liberating):

| | `PRIMARY_ATTESTED` | `SECONDARY_ATTESTED` | `INFERRED` | `AUTHORED_PROVISIONAL` |
|---|---|---|---|---|
| **binding** | 20 | 2 | 6 | 6 |
| **liberating** | 6 | 0 | 7 | **21** |
| total | 26 | 2 | 13 | 27 |

**The binding side is largely attested (20/34 primary); the liberating side is largely authored (21/34
provisional, only 6/34 primary).** This matches the table's own caveat — *"The LIBERATING pole is author
counter-rewritten for most varṇas — not attested."* The liberating poles exist because the framework supplies a
"liberated mode" for each attested distortion, **not** because a primary text states one.

### A.3 Special-attention varṇas

- **`ha` — `BACK_FIT`.** Both poles `AUTHORED_PROVISIONAL`. The table states the split is *"researcher-imposed;
  motivated partly by making 'happy' cohere."* This is an admitted back-fit toward an English target word and must
  be frozen + pre-registered before any pole-test authoring.
- **`pa` — `META_STALE_PA` (contradiction D1).** Binding `PRIMARY_ATTESTED` (ghṛṇā = the fetter of hatred, v3
  option B). The `important_caveats` block still says pa *"PARTIALLY INVERTS the source-attested pole"*, but the
  `pa` entry says v3 follows the attested assignment and *"no inversion flag remains."* Two source fields disagree.
- **`ttha` (ṭha) — `META_STALE_TTHA` (contradiction D2).** Binding `PRIMARY_ATTESTED` (anutāpa). The caveat calls
  the night/moon-vs-"Repentance" reading *"unresolved"*; the entry resolves it (anutāpa is the vṛtti; night/moon
  are associations). Two source fields disagree.
- **`sha` / `ssa` — `SIBILANT_SWAP` (D5).** Primary text: śa(sha)=tamoguṇa/kāma, ṣa(ssa)=rajoguṇa/artha; but v2
  **and** the lexicon swap them. v3 follows the primary text for the *name*, yet `ssa`'s **pole texts are imported
  from the lexicon's mis-filed 'sha' entry** — a documented provenance hazard (both `ssa` poles marked authored).
- **`tta` / `dda` — `REACHABILITY_CONTRADICTION` (D3).** Both poles for `tta` are `PRIMARY_ATTESTED` (vitarka vs
  pramita vāk). The table marks them `practically_reachable=False`; the active bridge emits them anyway
  (`true→ṭa`, `drum→ḍa`). Code vs table.
- **`tha` — `DOC_VS_CODE_TH` (D4).** Table marks it reachable, but the active `thfix` routes English `th` (/θ,ð/)
  to **ta**, so `tha` is **never emitted** by the active bridge. `active_status = INACTIVE_CONTRADICTS_TABLE`.

---

## B. Active vs inert (what the current bridge really uses)

| active_status | count | varṇas |
|---|---|---|
| `ACTIVE` (code emits, table agrees) | 18 | ba ca da ga ha ja ka la ma na nga pa ra sa sha ta va ya |
| `ACTIVE_CONTRADICTS_TABLE` | 2 | **dda, tta** (code emits; table says unreachable — D3) |
| `INACTIVE_CONTRADICTS_TABLE` | 1 | **tha** (table says reachable; code routes th→ta — D4) |
| `INACTIVE` | 13 | bha cha ddha dha gha jha kha ksha nna nya pha ssa ttha (aspirates + clusters the G2P never emits) |

- **20 varṇas are emittable by the active bridge** (18 clean + tta/dda). Of those, **only 11 are rendered in the
  facet map** (`ba da ga ka la ma na pa ra ta tta`) — the ceiling that made Gate G0 `NOT_TESTABLE`.
- The `practically_reachable` field in the frozen table is **stale for at least three varṇas** (tta, dda, tha)
  relative to the bridge actually wired into the tests.

---

## C. Phonological / composition rules

17 rules classified (`rule_provenance_register.json`):

| status | count | rules |
|---|---|---|
| `OUT_OF_SCOPE` | 7 | vowels_dropped, aspiration_excluded, sandhi_none, gemination_collapsed, order_discarded, multiplicity_discarded, position_discarded |
| `CONTRADICTORY` | 4 | consonant_clusters_presplit, retroflex_before_r, th_to_ta, orthography_over_phonology |
| `INFERRED` | 2 | dh_inert, transliteration_normalization |
| `MISSING` | 2 | anusvara_ignored, visarga_ignored |
| `AUTHORED_PROVISIONAL` | 2 | polarity_selection, facet_union |

The four `CONTRADICTORY` rules are the mechanism's load-bearing conflicts:
- **`retroflex_before_r` / `consonant_clusters_presplit`** — the table says clusters pre-split to dental da/ta and
  retroflex ṭa/ḍa are *"never produced"*; the active bridge's retroflex rule **does** produce them (D3).
- **`th_to_ta`** — active bridge maps `th`→ta; the meta caveat still says `th`→tha (D4).
- **`orthography_over_phonology`** — the stated intent (pronunciation/Sanskrit-first) conflicts with the actual
  **spelling-driven** pipeline (`faith`→ta, `doubt`→da+ba+ta come from English letters, not Sanskrit sounds).

The most consequential `OUT_OF_SCOPE` rule is **`order_discarded`**: the word rule is an **unordered, deduped set**
of consonant varṇas with **facet union** — sequence, position, and multiplicity are all thrown away. This is the
composition assumption the hypothesis most depends on, and it is not attested.

---

## D. Word-level composition — three layers, kept distinct

Per the guardrail, the layers are **not silently reconciled** (`word_composition_layers.json`):

1. **What the code does** (`OUT_OF_SCOPE`): spelling→G2P→consonant-only varṇas (vowels dropped, aspiration
   excluded), retroflex-before-r, th→ta, collapse to an unordered deduped set, union of 11-varṇa facet renders.
2. **What the docs claim** (`AUTHORED_PROVISIONAL`): a word's resonance is the composition of its varṇa poles; the
   table supplies the poles. The docs do **not** establish that an unordered facet-union over a spelling-derived
   consonant set recovers word meaning.
3. **What the hypothesis requires** (`UNRESOLVED`): a pronunciation-based, attested-meaning-faithful,
   order/position/multiplicity-sensitive composition. Layer 1 meets **none** of these.

---

## E. Discrepancy summary

**Four unresolved cross-artifact contradictions** (must be resolved before any inventory decision):

| id | kind | conflict |
|---|---|---|
| **D1** | doc vs doc | `important_caveats` says `pa` inverts the attested pole; the `pa` entry says no inversion remains. |
| **D2** | doc vs doc | `important_caveats` calls `ṭha` unresolved; the `ttha` entry resolves it (anutāpa = vṛtti). |
| **D3** | code vs table | `tta`/`dda` marked `practically_reachable=False`; the active bridge emits them. |
| **D4** | code vs doc | caveat says `th`→tha; active `thfix` routes `th`→ta (so `tha` is never emitted). |

**Two documented provenance hazards** (recorded, not blocking on their own):

| id | kind | note |
|---|---|---|
| **D5** | provenance hazard | `SIBILANT_SWAP`: `ssa` pole texts imported from the lexicon's mis-filed 'sha' entry. |
| **D6** | provenance hazard | `BACK_FIT`: `ha`'s split is researcher-imposed, partly to make 'happy' cohere. |

---

## F. Validation

- All **34** varṇas accounted; **68** poles classified (26 primary + 2 secondary + 13 inferred + 27 authored).
- **17** rules classified; **3** missing inventory categories (vowels, anusvāra, visarga).
- Active-status counts reconcile to 34 (18 + 2 + 1 + 13). Emittable = 20; rendered = 11.
- Counts are recomputed by the generator on each run and emitted into `varna_provenance_register.json#validation`.
- No frozen artifact, run01, Track G, or mapping was read for mutation or modified; the register is additive only.

## G. Readiness verdict and single next action

**`BLOCKED_BY_SOURCE_CONTRADICTIONS`.** The four cross-artifact contradictions (D1–D4) are genuine conflicts
about what the mechanism *is*, not gaps in what it *means*. They block a clean inventory decision because the
inventory would inherit an unstable base (e.g. is `tta`/`dda` in or out? does `th` mean melancholy or staticity?).
The secondary blocker is provenance depth: **21/34 liberating poles are authored** and the entire vowel inventory
is `MISSING`.

**Single recommended next action:** resolve the four enumerated source/code contradictions (D1–D4) — reconcile the
`important_caveats` block with the per-varṇa entries (D1, D2) and re-sync the `practically_reachable`/`th`-routing
fields to the **active** bridge (D3, D4) — **before** any inventory decision. Do not author, promote, or freeze any
pole in the process.
