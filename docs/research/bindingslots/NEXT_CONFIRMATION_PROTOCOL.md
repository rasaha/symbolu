# Next phase

The mechanical verdict `ROUTING_PURITY_NOT_RESOLVED` selects the next phase.

## Exact next phase: BindingSlots Shortcut-Path Isolation

Confirm the pathway carrying the surviving retrieval and then test the persistence levers this screen
localized. Preregistered, frozen causal gate reused, Stage-2 reserve seeds 23–27 (not the Stage-1
seeds), no tuning of what is being measured.

1. **Confirm the diffuse slot-read pathway.** The Stage-1 ablations already indicate it (retrieval
   collapses under slots-off, survives randomized-addressing → not window-leak, not token-duplication).
   The isolation phase adds the standing shortcut controls (local-window reduction, value-token dedup,
   positional controls) to rule the alternatives out explicitly.

2. **Test persistence of address-dependence through withdrawal** — the deferred levers the mechanism
   points to, one per arm, under the frozen causal gate:
   - **O1R** — a standing (non-decaying) small correct-slot residual term through steps 601–1200,
     so address-dependence is not lost at withdrawal;
   - **H2** — a functional teacher snapshot at step 600 that consolidates the scaffold-era retrieval
     representation across the handoff;
   - **H1** — routing-parameter learning-rate consolidation across the handoff;
   - **C1** — a preregistered objective + persistence combination, only if a single lever clears its
     subgate.

The success criterion is unchanged: causally clean, retained routing (correct-slot prob/rank/margin
retained at 1200 **and** collapse under both slots-off and randomized-addressing) on ≥ 4/5 seeds,
beating R0's clean-stable count.

## Not started here

This PR does not begin the isolation phase, does not tune O1/O2/H3, and does not run the deferred
arms. Readiness remains `KDA_VALIDATION_BLOCKED`.
