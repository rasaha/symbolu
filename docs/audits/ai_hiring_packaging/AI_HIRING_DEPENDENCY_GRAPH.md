# AI Hiring — Dependency Graph

Machine-readable: [`ai_hiring_dependency_graph.json`](ai_hiring_dependency_graph.json).
Generated by an AST scan of every module in the canonical `ugence_ai_hiring`
package (211 source files, 1,428 import edges).

## Import classification summary
| Classification | Edges |
|---|---|
| `INTERNAL_RELATIVE` (`from .x import`) | 770 |
| `STDLIB` | 459 |
| `UGENCE_PACKAGE_DEPENDENCY` | 111 |
| `INTERNAL_ABSOLUTE` (`ugence_ai_hiring.*`) | 41 |
| `DECLARED_RUNTIME_DEPENDENCY` (`pydantic`) | 40 |
| `DECLARED_OPTIONAL_DEPENDENCY` (`fastapi`) | 7 |
| **`FORBIDDEN`** | **0** |
| `UNKNOWN_THIRD_PARTY` | 0 |

## External (non-stdlib, non-internal) roots
| Root | Classification | Migration decision |
|---|---|---|
| `pydantic` | DECLARED_RUNTIME_DEPENDENCY | keep (hard dep, `>=2`) |
| `ugence_decision_authority` | UGENCE_PACKAGE_DEPENDENCY | depend on `ugence-decision-authority>=1.0.0` |
| `ugence_governance_provider_framework` | UGENCE_PACKAGE_DEPENDENCY | depend on `ugence-governance-provider-framework>=0.1.0` |
| `fastapi` | DECLARED_OPTIONAL_DEPENDENCY | keep behind the `api` extra (imported lazily) |

`ugence-governance-contracts` is a transitive dependency of the two Ugence
packages above and is also declared directly for explicitness.

## Hidden / forbidden dependencies
- **Repository-internal hidden dependencies found: 0.** No import of `symbolu`,
  `agentic`, `cloud_controller`, `hybrid_llm_vnext_lab`, `experiments`,
  `applications`, or `domains` remains in the canonical package.
- **Legacy compat namespaces:** the repo-root `decision_governance` and
  `governance_providers` shims are **not** imported; the canonical package imports
  the canonical `ugence_*` packages directly (the shims do not ship in the wheel).
- **NumPy:** removed — no AI Hiring code path imports it, so it is not a
  dependency of the independent wheel.
- **Vendor model SDKs / DB drivers / cloud clients:** none imported.

Enforced continuously by
`tests/packaging/test_dependency_boundaries.py` (AST boundary tests).
