"""
FSCS-V Benchmark Suite.

Validates unresolved patent integration gaps and provides
end-to-end evaluation for the FSCS-DIFFUSION v3.2 specification.

Run all benchmarks:
    python -m symbolu.vision.video.benchmarks.run_all

Individual benchmarks:
    python -m symbolu.vision.video.benchmarks.bench_rectification
    python -m symbolu.vision.video.benchmarks.bench_l2_phase_lock
    python -m symbolu.vision.video.benchmarks.bench_three_band_ablation
    python -m symbolu.vision.video.benchmarks.bench_proxy_encoder
    python -m symbolu.vision.video.benchmarks.bench_fvd
    python -m symbolu.vision.video.benchmarks.bench_identity_lock
    python -m symbolu.vision.video.benchmarks.bench_scale
    python -m symbolu.vision.video.benchmarks.bench_gradient_safety
"""
