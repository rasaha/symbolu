"""
Benchmark runners for specialized diagnostic tests.

Each module provides a standalone benchmark with its own CLI arguments.
Benchmarks are invoked via flags on the main train_hard_probes.py script.
"""
