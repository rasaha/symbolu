"""Symbol-U complementarity probe — discovery-stage measurement harness.

EXPERIMENTAL / ISOLATED. This package measures whether the Symbol-U variable `U`
carries semantic information COMPLEMENTARY to a Transformer sentence embedding
`E` — before any deployment architecture is built. It does NOT modify, replace,
or depend on the older clean_softmax detector files, which remain canonical.

See README.md and SYMBOL_U_RESEARCH_STRATEGY.md (in ../clean_softmax/).
"""
