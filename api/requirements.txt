# Runtime dependencies for the Vercel-hosted Governance Studio function.
# Deliberately minimal: the local packages are put on sys.path by index.py, and
# the repository-root requirements.txt (training/research deps) must NOT be used
# for this function.
fastapi>=0.110
starlette>=0.36
uvicorn>=0.27
pydantic>=2
argon2-cffi>=23.1
