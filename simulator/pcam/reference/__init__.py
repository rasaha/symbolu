"""
PCAM vendored references.

This subpackage holds in-tree copies of upstream specifications that the
PCAM parity harness uses as oracles. Vendored files are read-only
specification references; they are not the runtime implementation.

See ``attention_evictor_vendored.py`` for the CTM+ KV-cache policy
reference and ``Project_documentation/simulator/simulator/pcam/docs/VENDORED_REFERENCE_UPDATE_RITUAL.md``
for the update ritual.
"""
