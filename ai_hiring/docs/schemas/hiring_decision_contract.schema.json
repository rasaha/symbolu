{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ugence.ai/schemas/hiring/hiring_decision_contract.schema.json",
  "title": "HiringDecisionContract",
  "description": "The versioned, compiled policy object the Decision Authority evaluates for a role. Hiring specialization of the platform Decision Contract / RiskDecisionCase. The Decision Authority evaluates THIS, not scores. The Policy Compiler rejects any contract that references the numeric Overall Fit Index in a gate/eligibility rule, makes a mandatory gate compensable, omits required evidence for a weighted dimension, or admits an AI principal into the approval chain.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contract_id",
    "version",
    "status",
    "role_ref",
    "mandatory_gates",
    "dimension_weights_ref",
    "evidence_requirements",
    "confidence_thresholds",
    "review_schedule",
    "approval_chain",
    "compilation"
  ],
  "properties": {
    "contract_id": { "type": "string" },
    "version": { "type": "integer", "minimum": 1, "description": "Immutable once PUBLISHED." },
    "status": { "type": "string", "enum": ["DRAFT", "IN_REVIEW", "APPROVED", "PUBLISHED", "DEPRECATED"] },
    "role_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": ["job_definition_id", "rcp_id", "rcp_version"],
      "properties": {
        "job_definition_id": { "type": "string" },
        "rcp_id": { "type": "string" },
        "rcp_version": { "type": "integer", "minimum": 1 }
      }
    },
    "mandatory_gates": {
      "type": "array",
      "minItems": 0,
      "items": { "$ref": "mandatory_gate.schema.json" },
      "description": "Non-compensatory gates evaluated for eligibility."
    },
    "dimension_weights_ref": {
      "type": "string",
      "description": "Reference to the RCP dimension weights (advisory OFI only)."
    },
    "evidence_requirements": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["dimension", "required_types"],
        "properties": {
          "dimension": { "type": "string" },
          "required_types": { "type": "array", "items": { "type": "string" } },
          "admissibility_policy": { "type": "string", "description": "TAP admissibility policy id applied to this dimension's evidence." }
        }
      }
    },
    "confidence_thresholds": {
      "type": "object",
      "additionalProperties": { "type": "number", "minimum": 0, "maximum": 1 },
      "description": "Per-dimension minimum confidence (mirrors RCP minimum_confidence)."
    },
    "review_schedule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["checkpoints_months"],
      "properties": {
        "checkpoints_months": {
          "type": "array",
          "items": { "type": "integer", "minimum": 1 },
          "default": [1, 3, 6, 12],
          "description": "Post-hire review cadence feeding calibration."
        }
      }
    },
    "approval_chain": {
      "type": "array",
      "minItems": 1,
      "description": "Ordered human approver roles authorized to turn a recommendation into a binding decision. AI principals are rejected at compile time.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["approver_role", "actor_type"],
        "properties": {
          "approver_role": { "type": "string" },
          "actor_type": { "const": "HUMAN" }
        }
      }
    },
    "compilation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["compiled", "checks"],
      "properties": {
        "compiled": { "type": "boolean" },
        "compiler_version": { "type": "string" },
        "checks": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "no_ofi_in_policy": { "type": "boolean", "description": "true = no gate/eligibility rule reads the numeric OFI." },
            "gates_non_compensatory": { "type": "boolean" },
            "weighted_dimensions_have_required_evidence": { "type": "boolean" },
            "approval_chain_human_only": { "type": "boolean" }
          }
        }
      }
    }
  }
}
