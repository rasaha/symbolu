node_modules/
dist/
dist-ssr/
coverage/
playwright-report/
test-results/
*.local
.eslintcache
.vite/
artifacts/dependency-audit-report.json

# The repo-root .gitignore ignores any `lib/` directory (a Python build-artifact
# rule). Our application source lives in `src/lib/`, so re-include it here.
!src/lib/
!src/lib/**
frontend-build.json
