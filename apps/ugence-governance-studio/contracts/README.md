# contracts/

Reserved for the frozen OpenAPI contract (`openapi.json`) and its drift verifier,
produced in **P3B** (Deterministic Demo API). Empty in P3A by design — P3A defines
the data contract (`../demo_data/` + `../expected_outputs/`), not the HTTP contract.
